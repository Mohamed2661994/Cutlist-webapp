- [x] Verify that the copilot-instructions.md file in the .github directory is created.

- [x] Clarify Project Requirements
	- React web app using Vite + TypeScript.
	- UI components will come from shadcn/ui.
	- Target product is a cabinet cut list optimizer with 3D preview.

- [x] Scaffold the Project
	- Creating the Vite React TypeScript workspace in the current folder.

- [ ] Customize the Project

- [x] Install Required Extensions
	- No extra VS Code extensions were required by the workspace setup flow.

- [x] Compile the Project
	- `npm run build` passes.
	- `npm run lint` passes.

- [x] Create and Run Task
	- Added `.vscode/tasks.json` with a `dev` task that runs `npm run dev`.

- [x] Launch the Project
	- Development server started through the VS Code `dev` task on `http://localhost:5173`.

- [x] Ensure Documentation is Complete
	- Updated `README.md` with the current stack and scripts.
	- Cleaned this file to reflect the current workspace state.

## Execution Guidelines
- Work through each checklist item systematically.
- Keep communication concise and focused.
- Follow development best practices.